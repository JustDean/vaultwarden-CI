{{ with .Values.vaultwarden.admin.basicAuth }}
{{ if eq .enabled true }}
apiVersion: v1
metadata:
  name: basic-auth
data:
  auth: {{ htpasswd .username .password | b64enc | quote }}
kind: Secret
type: Opaque
{{ end }}
{{ end }}