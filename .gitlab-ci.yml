stages:
  # - lint
  # - build
  - deploy-test
  # - deploy-prod

variables:
  IMAGE: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG
  VERSION: 11

# Lint helm:
#   stage: lint
#   image: dtzar/helm-kubectl:3.10.0
#   script:
#     - helm lint .kube/vaultwarden


# Build:
#   stage: build
#   image: docker:20.10.12
#   services:
#     - docker:20.10.12-dind
#   before_script:
#     - i=0; while [ "$i" -lt 12 ]; do docker info && break; sleep 5; i=$(( i + 1 )) ; done
#   script:
#     - docker login $CI_REGISTRY -u gitlab-ci-token -p $CI_BUILD_TOKEN 
#     - docker build -t $IMAGE:$VERSION .
#     - docker push $IMAGE:$VERSION

.create_namespace:
  allow_failure: true
  before_script:
    - kubectl create namespace $NAMESPACE

.create_secret:
  extends: .create_namespace
  allow_failure: true
  before_script:
    - >
      kubectl create secret docker-registry registry-credentials 
      --docker-server=$CI_REGISTRY 
      --docker-username=$CI_DEPLOY_USER
      --docker-password=$CI_DEPLOY_PASSWORD
      -o yaml --dry-run=client | kubectl -n $NAMESPACE apply -f - || true
  script:
    - > 
        helm -n $NAMESPACE 
        --set vaultwarden.image.repository=$IMAGE vaultwarden.ingress.domain=$DOMAIN 
        upgrade --install vaultwarden .kube/vaultwarden

Deploy Test:
  stage: deploy-test
  extends: .create_secret
  # needs:
  #   - Build
  variables:
    NAMESPACE: vaultwarden-test
    DOMAIN: vaultwarden-test.dolinskiydmitri-552.metaclass-st.ru
  image: dtzar/helm-kubectl:3.10.0

# Stop Test:
#   # TODO

# Deploy Prod:
#   stage: deploy-prod
#   variables:
#      DOMAIN: vaultwarden-prod.dolinskiydmitri-552.metaclass-st.ru