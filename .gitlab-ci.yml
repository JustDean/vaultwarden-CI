stages:
  # - lint
  # - build
  - deploy-test
  # - deploy-prod

variables:
  IMAGE: $CI_REGISTRY_IMAGE/$CI_COMMIT_REF_SLUG:11

# Lint helm:
#   stage: lint
#   image: dtzar/helm-kubectl:3.10.0
#   script:
#     - helm lint .kube/vaultwarden


# Build:
#   stage: build
#   image: docker:20.10.12
#   services:
#     - docker:20.10.12-dind
#   before_script:
#     - i=0; while [ "$i" -lt 12 ]; do docker info && break; sleep 5; i=$(( i + 1 )) ; done
#   script:
#     - docker login $CI_REGISTRY -u gitlab-ci-token -p $CI_BUILD_TOKEN 
#     - docker build -t $IMAGE .
#     - docker push $IMAGE

Deploy Test:
  stage: deploy-test
  # needs:
  #   - Build
  variables:
    NAMESPACE: vaultwarden-test
  image: dtzar/helm-kubectl:3.10.0
  before_script:
  - >
    - kubectl create secret docker-registry registry-credentials \
    -- docker-server=${CI_REGISTRY} \
    -- docker-username=${CI_DEPLOY_USER} \
    -- doccker-password=${CI_DEPLOY_PASSWORD} \
    -o yaml --dry-run | kubectl apply -n $NAMESPACE -f -
  script:
  - helm -n vaultwarden-test upgrade --install vaultwarden .kube/vaultwarden

# Stop Test:
#   # TODO

# Deploy Prod:
#   # TODO